@{
  Layout = "_Layout";
}

@model SweetShop.Models.Treat

<h2>Confirm treat delete?</h2>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Description)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to Treats", "Index")